{
  "name": "ai-war-core",
  "version": "0.1.0",
  "devDependencies": {
    "jq": "^1.7.2",
    "node-jq": "^1.3.1",
    "truffle": "^4.1.14"
  },
  "scripts": {
    "build": "truffle compile",
    "test": "truffle --network ${NETWORK:-local} test",
    "truffle": "truffle --network ${NETWORK:-local}"
  }
}
